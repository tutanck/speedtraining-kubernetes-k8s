apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: speedtraining-back
  labels:
    app: speedtraining-back
spec:
  rules:
    - host: speedtraining-back-mferraina.apps.codeinlove.fr
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: speedtraining-back
                port:
                  number: 80
